/* MarineIQ — REF_BOOKS Assembler
   Combines the 3 separate book consts into one lookup object
   Requires: diesel-handbook.js, aranha.js, aux-machinery.js loaded first */

const REF_BOOKS = {
  dh: REF_BOOK_DH,
  ar: REF_BOOK_AR,
  am: REF_BOOK_AM,
};
